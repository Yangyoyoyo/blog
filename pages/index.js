import Layout from '../components/Layout';

export default class Index extends React.Component {
    render() {
        return (
            <Layout>
                <h2 className="page-header">我是中文字体</h2>
            </Layout>
        );
    }
}
